{
  "mcpServers": {
    "druid-mcp-server-http": {
      "type": "http",
      "url": "http://localhost:8080/mcp",
      "headers": {
        "Authorization": "Bearer ${MCP_OAUTH_TOKEN}"
      }
    }
  }
}
